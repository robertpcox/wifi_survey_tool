

Deployment: Static Nginx with Node used only during build, and no runtime backend?Static nginx with node during the build, so it makes it easy.
Technology: Is Vite + TypeScript acceptable, or must it remain plain HTML/JavaScript with no build dependencies?So, thats the question, hmm, can we discuss further
150-line rule: Does it cover HTML and CSS too? May schemas, test fixtures and generated output exceed it?so, its about future agent work, so unto 500 max, exported to an MD for you and the explanation /Users/robert/Git/wifi_survey_tool/current_tool/Scope/coding_pattern.md
Pages: Exactly four separate applications—Creator, Runner, Player and Report—or should there also be a landing/library page?so the greater / runner will be seperate stand alone apps. (One admin to author) and the customer to run the other one.with the landing page.Maybe on boot it picks up the surveys that are then and then creates the launch with Customer and when selected it makes the report only show for that customeridso it means that actually its set by customer  id in the url.so the index page would only show the customer, so a dashboard.you see the surveys run- launch the player- launch the report page
Devices: Creator desktop-only and Runner iPhone-first? Must Player and Report also work well on mobile?Only the runner of iPhone and android. Needs to be super light touch, and easy to use, load the page, so, each survey has a unique id (so they all the together | thats needed for the compare… aarrrgh, so lets in the first pass, just have the reports selectable.
Position sources: Which are required for v1: MazeMap Cloud, LiPi and Cisco DNA Spaces? Can multiple sources run simultaneously?v1 is just Cloud, but we should make sure the plumbing is there for other ones, so its about the poller is the poller, and that it is just feed a standard pattern.
Secrets: Should access tokens and positioning keys live only in memory, or may sessionStorage retain them until the tab closes? My recommendation is sessionStorage, never exports or localStorage.no, only in memory, for both the maps and the capture, don’t want to leak it
Survey metadata: Are these mandatory: customer name/ID, campus name/ID, building, timezone, survey name/ID, route name/version, operator and enabled sources?Customer name, and customer it, campus name and id, timezone, survey name and it, buildings tested, zlevels, zlevel mapping, positioning source.
Route contract: Confirm definitions embed stops, exact ordered legs and generated check-ins so the Runner never requests a fresh route. Retain the rule: exact lng/lat/z, nullable poiId, POI name and outdoors status?Correct, the runner is getting the paths already set on the map, including the check points, so it just loads the survey file and off it goes, once you enter the required details, and then you click go.So we should output the length and the time of the survey (we pick it up from the routes and the wait time, this survery is 4 floors, 2 km, and will take around 45mins.so it means we can rerun the exact same test each and every time, and that a change is a new survey id.Which means we should add customer_id and campus_id to the results file name, so we can load based on customer id or campus id.
Recovery: Should the Runner continuously autosave to IndexedDB and offer “Resume interrupted survey”? Strongly recommended for iPhone.We need to make it for both and which ever is the most light weight to make it easy. So the pool rate fyi is covered in the setup not the runner.
Folder workflow: Is “copy JSON into surveys/ or results/, then run npm run build” acceptable? Truly automatic folder discovery requires a small server endpoint.100%, build is the way to go, trying to not have to load scripts, or have packages that are needed, because with recent hacks, we want to be safe.
Comparisons: Compare only runs with the same surveyId and route hash? Two runs, or up to four?compare only the same routes | and the oldest is the base line.
Heatmaps: Which selectable layers are required: positional error, frozen duration, fix age/staleness, wrong-floor duration and update frequency?so with heat maps, is to see frozen position in the first pass v1 | its so you can look at the map as see which areas have the most problems. Or if there is a better way I am all eyes, its how we can show on the map the areas where there are issues. The floor switching ends up in the same area. Its about the sticky client, so we have the pool rate, so if we went room to room, where we have a pool and no position change it gets a dot no?
Compatibility: Must Player and Report support existing v1/v2 exports, while Creator and Runner produce a new v3 format?
all new v3 format end to end.and we should set milestones and key parts, so we don’t burn.with the first pass cleaning up the file in a split, to shave tokens at coding no?