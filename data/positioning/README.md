# Recorded positioning responses

`ndh-outpatient-level-00.mazemap-cloud.response.json` proves the live NDH Level 00
provider body normalizes without losing provider fields and passes route preflight when fresh.

The fixture contains no credentials. It is a standalone response body, so it does not prove
the Runner's HTTP status, request/response timing, browser proxy path, or a completed field run.
